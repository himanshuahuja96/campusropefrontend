/*
 * UserSearch Messages
 *
 * This contains all the text for the UserSearch component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UserSearch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserSearch component!',
  },
});
