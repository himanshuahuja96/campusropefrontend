/*
 * AssignAdminTask Messages
 *
 * This contains all the text for the AssignAdminTask container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssignAdminTask';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssignAdminTask container!',
  },
});
